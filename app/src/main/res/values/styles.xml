<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bootstrap_brand_success</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/bootstrap_brand_success</item>
    </style>

    <style name="TextViewStyle" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="colorControlNormal">@color/bootstrap_brand_success</item>
    </style>

</resources>
